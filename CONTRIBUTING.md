# دليل المساهمة في المشروع (Contributing Guide)

يسعدنا اهتمامك بالمساهمة في تطوير مشروع **BluePrint ERP**. يهدف هذا المستند إلى توضيح المعايير والإجراءات البرمجية التي نتبعها لضمان جودة واستقرار الكود.

---

## 🛠️ بيئة التطوير المحلية (Local Development Setup)

1. **تثبيت الاعتمادات:**
   ```bash
   bun install # أو npm install
   ```

2. **تشغيل خادم التطوير:**
   ```bash
   bun run dev # أو npm run dev
   ```

3. **تشغيل الفحوصات ولينت الكود:**
   ```bash
   bun run lint
   bun run type-check
   bun run test
   ```

---

## 🌿 إستراتيجية الفروع والعمل (Git Branching Strategy)

نحن نتبع أسلوب فروع الميزات المبسط (Feature Branching):
* **`main`**: فرع الإنتاج المستقر. لا يتم الدمج فيه مباشرة إلا عبر Pull Request معتمد.
* **`develop`**: الفرع الرئيسي للتطوير والدمج اليومي.
* **فروع الميزات (`feature/*`)**: تُنشأ من فرع `develop` لتطوير ميزة معينة (مثال: `feature/add-sso`).
* **فروع الإصلاحات (`fix/*`)**: تُنشأ لإصلاح الأخطاء المكتشفة (مثال: `fix/jwt-expiration`).

---

## 📝 تنسيق رسائل الالتزام (Conventional Commits)

نلتزم بمعيار Conventional Commits لتسهيل تتبع التغييرات وإنشاء سجل التغييرات التلقائي. يجب أن تتبع الرسائل الصيغة التالية:

```
<type>(<scope>): <subject>
```

أنواع الالتزام المتاحة (`types`):
* **`feat`**: إضافة ميزة جديدة للتطبيق.
* **`fix`**: إصلاح خطأ برمجي.
* **`docs`**: تحديث التوثيق فقط.
* **`style`**: تغييرات لا تؤثر على عمل الكود (تنسيق، فواصل منقوطة، إلخ).
* **`refactor`**: تعديل الكود لتحسين هيكليته دون تغيير وظيفته.
* **`test`**: إضافة اختبارات جديدة أو تعديل اختبارات قائمة.
* **`chore`**: تحديث إعدادات البناء أو الحزم البرمجية دون تغيير كود التطبيق.

أمثلة:
- `feat(auth): add step-up 2FA on sensitive routes`
- `fix(chat): solve memory leak on connection closed`
- `docs(readme): update deployment instructions`

---

## 🔍 جودة الكود والمعايير البرمجية (Coding Standards)

* **الالتزام بالـ Linter:** يجب ألا يحتوي الكود على أي تحذيرات أو أخطاء من ESLint. تأكد من تشغيل `npm run lint` قبل رفع التعديلات.
* **سلامة الأنواع (TypeScript):** تجنب استخدام النوع `any`. يجب كتابة الواجهات والأنواع بدقة وتجنب استخدام التعليقات `@ts-ignore` أو `@ts-nocheck` إلا في الضرورة القصوى الموثقة.
* **التوافق اللغوي (i18n):** يجب تعريف جميع النصوص القابلة للعرض في ملفات الترجمة `ar.json` و `en.json` وعدم كتابة النصوص مباشرة بالواجهات.
* **حماية التعددية (Multi-tenancy):** تأكد دائماً من تصفية البيانات باستخدام حقل `organizationId` في جميع استعلامات قواعد البيانات لتجنب تسريب البيانات بين المنظمات.
